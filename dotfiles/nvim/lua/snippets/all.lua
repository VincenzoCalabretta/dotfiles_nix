-- Global snippets (available in every filetype). For filetype-specific
-- snippets, add a sibling file named after the filetype, e.g. lua.lua,
-- python.lua, cpp.lua -- the filename is how LuaSnip's from_lua loader
-- maps a file to a filetype.
local snippets = {}
local autosnippets = {}

return snippets, autosnippets
