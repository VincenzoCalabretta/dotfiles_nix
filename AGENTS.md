# Agent instructions

## CHECKLIST.md

When you add, remove, or change any deployment step — secret provisioning,
deployment tooling, CI workflow changes, new host requirements, or anything
else a human would need to do manually — update `CHECKLIST.md` in the same
change. Every diff that touches how the system is deployed should have a
matching diff in the checklist.